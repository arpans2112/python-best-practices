word = "python"

test = False
if test:
    print("we are working")

if test:
    print(word[:2])

if not test:
    str1 = "helloworld"
    # here ::n : returns string by picking [n character from the beginning of the string.
    # here ::-n : returns string by picking [n character from the end of the string. if -n = -1 , it will return the reverse string
    # 
    newstr = str1[::-1]
    print(newstr)















